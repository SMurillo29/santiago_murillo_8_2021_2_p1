import 'package:flutter/material.dart';

class DetailCountie extends StatelessWidget {
  const DetailCountie({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(backgroundColor: Color.fromRGBO(237, 237, 237, 25),title: Text('Detail'),),
    );
  }
}